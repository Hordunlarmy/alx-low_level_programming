#include "main.h"


/**
 * _abs - Entry point
 * @c: ineger to be computed
 * Return: computes the absolute value of an integer
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
		return (-c);
}
