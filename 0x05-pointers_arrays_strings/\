#include "main.h"


/**
 *  _puts - Entry point
 * @str: variable name
 * Return: a string, followed by a new line, to stdout.
 */
void print_rev(char *s)
{
        int i;

        for (i = 20; s[i] != '\0'; i--)
	{
		_putchar(s[i]);
	}
        _putchar('\n');
}
