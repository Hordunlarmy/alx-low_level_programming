#include <stdio.h>


/**
 * main - Entry point
 * @argc: arg count
 * @argv: arg array
 * Return: the number of arguments passed into it.
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
		printf("%d\n", argc - 1);
	return (0);
}
